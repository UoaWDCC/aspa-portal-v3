import { Metadata } from "next";

export const metadata: Metadata = {
  title: "ASPA - Log In",
  description: "ASPA Log In Page",
};

export default function Login() {
  return (
    <div>
      Login
    </div>
  );
}
